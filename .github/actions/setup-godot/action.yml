name: Setup checksummed Godot
description: Install the supported Godot 4.7.2 Linux editor from checksum-verified bytes

runs:
  using: composite
  steps:
    - name: Install checksummed Godot 4.7.2
      shell: bash
      run: ./scripts/install-godot.sh "$RUNNER_TEMP/gd-redis-godot"
